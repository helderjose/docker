docker container run -d --name nginx nginx
docker container run -d --name mysql -e MYSQL_RANDOM_ROOT_PASSWORD=yes mysql
docker container ls

docker container top mysql
docker container inspect mysql

docker container stats --help
docker container stats


----- docker container top -----
mostra os processos que estão sendo executado em um container


----- docker container inspect -----
show metadata about the container (startup, config, volumes, networking, etc)


----- docker container stats -----
show live performance data for all containers